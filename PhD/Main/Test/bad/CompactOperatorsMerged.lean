import PhD.Main.TateFredholm.«08_BaseChange»
import PhD.Main.TateFredholm.«05_Noetherian»

/-!
# Moved: the merged compact-operator development now lives in `PhD/Main/TateFredholm/`

This file was the fourth (merged, most general) compact-operator blueprint.  It became
the primary development and was split into the module tree `PhD/Main/TateFredholm/` —
see `PhD/Main/TateFredholm/00_Tate.lean` for the overview, module map, dictionary, and
references.  This stub re-exports the whole development so existing
`import PhD.Main.Test.CompactOperatorsMerged` lines and build targets keep working.

The three parent blueprints remain as source documentation:
`PhD.Main.Test.CompactOperators` (Buzzard), `PhD.Main.Test.CompactOperatorsBellaiche`
(Bellaïche), `PhD.Main.Test.CompactOperatorsJohanssonNewton` (Johansson–Newton).
-/
